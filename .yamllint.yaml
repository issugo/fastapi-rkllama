# yamllint configuration file
extends: default

rules:
  document-start: disable
  line-length:
    max: 120
    level: warning
  brackets:
    min-spaces-inside: 0
    max-spaces-inside: 1
  truthy: disable
